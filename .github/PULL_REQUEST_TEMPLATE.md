<!-- Branch naming: <type>/<short-desc> or <type>/<id>-<short-desc>.
     Types: task|feat|bug|refactor|chore|docs|ci|test|perf (no fix/ — use bug/). -->

## Why

<!-- The problem or motivation. Link the issue with `Closes #<id>` if there is one. -->

## What

<!-- The change itself, briefly. -->

## Verification

<!-- How you checked it: `mise run check` output, manual steps, screenshots.
     If a workflow touched here does not run on pull requests (deploy/release),
     say so plainly instead of claiming green. -->

<!-- First contribution to an AGPL repository? Add yourself to
     .github/cla-signatures.json in this same PR — see CLA.md. -->
